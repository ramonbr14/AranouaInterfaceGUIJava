Implementando Action Listerner no Botao

1) Implemente a interface actionListerner
2) definir o metodo add actionListerner para o botao passando o mesmo parametro(this).
3) O metodo relacionado ao ActionListerner é o ActionPerformed. Esse é o Metodo em que seu codigo deve parecer.